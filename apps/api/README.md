# travel-planner-api

This directory is the intended future home of the FastAPI backend,
currently located in `src/` at the workspace root.

Migration plan: see `docs/12_ADR-frontend-monorepo-structure.md`.
